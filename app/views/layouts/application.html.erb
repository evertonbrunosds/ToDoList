<!DOCTYPE html>
<html>
  <head>
    <title>Todolist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js" integrity="sha384-BBtl+eGJRgqQAUMxJ7pMwbEyER4l1g+O15P+16Ep7Q9Q+zqX6gSbd85u4mG4QzX+" crossorigin="anonymous"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }
        
        html, body {
            height: 100%;
            margin: 0;
            padding-top: 20px;
        }

        .priority {color: #3498db}
        .priority.low {color: #3498db}
        .priority.medium {color: #f39c12}
        .priority.high {color: #e74c3c}
        .completion {color: #3498db}
        .completion.true {color: #3498db}
        .completion.false {color: #e74c3c}

        .vertical-bar::before {
            content: '|';
            margin: 0 10px; /* Ajuste a margem conforme necessário */
            color: #000; /* Cor da barra vertical */
          }

        center {
            justify-content: center;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-radius: 8px;
            width: 80%;
            align-items: center;
            margin: auto;
        }

        h1 {
            color: #333;
        }

        p {
            margin: 10px 0;
            color: #666;
        }

        .priority {
            color: #e44d26;
        }

        .completion {
            color: #34a853;
        }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
